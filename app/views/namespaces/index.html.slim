h1.page-header Namespace'y
-if can? :create, :user
 	=link_to 'Nowy namespace', new_namespace_path, class: "btn btn-primary "
div class=("alert alert-success" if flash[:notice]) role="alert" =flash[:notice]
table#users.table.table-striped
	thead
		tr
			th Nazwa
			th Opis
			th Usuń
			th Edytuj
	tbody 
	  -@namespace.each do |namespace| 
	    = render :partial => "tr", locals: {namespace: namespace}
